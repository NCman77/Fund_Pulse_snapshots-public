name: Watch international public market sessions

on:
  workflow_dispatch:
    inputs:
      watch:
        description: all, jp-morning, jp-afternoon, kr-morning, kr-afternoon, cn-morning, cn-afternoon, sg-morning, sg-afternoon, uk-morning, uk-afternoon, eu-morning, eu-afternoon, us-morning, or us-afternoon
        required: true
        default: all
        type: string
  schedule:
    - cron: '20 8 * * 1-5'
      timezone: Asia/Tokyo
    - cron: '20 11 * * 1-5'
      timezone: Asia/Tokyo
    - cron: '21 8 * * 1-5'
      timezone: Asia/Seoul
    - cron: '21 11 * * 1-5'
      timezone: Asia/Seoul
    - cron: '50 8 * * 1-5'
      timezone: Asia/Shanghai
    - cron: '20 12 * * 1-5'
      timezone: Asia/Shanghai
    - cron: '22 8 * * 1-5'
      timezone: Asia/Singapore
    - cron: '21 12 * * 1-5'
      timezone: Asia/Singapore
    - cron: '20 7 * * 1-5'
      timezone: Europe/London
    - cron: '22 11 * * 1-5'
      timezone: Europe/London
    - cron: '23 8 * * 1-5'
      timezone: Europe/Paris
    - cron: '50 12 * * 1-5'
      timezone: Europe/Paris
    - cron: '51 8 * * 1-5'
      timezone: America/New_York
    - cron: '51 12 * * 1-5'
      timezone: America/New_York

permissions:
  contents: write

jobs:
  jp_morning:
    if: ${{ (github.event_name == 'schedule' && github.event.schedule == '20 8 * * 1-5') || (github.event_name == 'workflow_dispatch' && (inputs.watch == 'all' || inputs.watch == 'jp-morning')) }}
    uses: ./.github/workflows/run-market-session-watcher.yml
    with:
      market: jp
      session_name: morning
      slots: 09:05,09:35,10:05,10:35,11:05,11:30
  jp_afternoon:
    if: ${{ (github.event_name == 'schedule' && github.event.schedule == '20 11 * * 1-5') || (github.event_name == 'workflow_dispatch' && (inputs.watch == 'all' || inputs.watch == 'jp-afternoon')) }}
    uses: ./.github/workflows/run-market-session-watcher.yml
    with:
      market: jp
      session_name: afternoon
      slots: 12:35,13:05,13:35,14:05,14:35,15:05,15:30
  kr_morning:
    if: ${{ (github.event_name == 'schedule' && github.event.schedule == '21 8 * * 1-5') || (github.event_name == 'workflow_dispatch' && (inputs.watch == 'all' || inputs.watch == 'kr-morning')) }}
    uses: ./.github/workflows/run-market-session-watcher.yml
    with:
      market: kr
      session_name: morning
      slots: 09:05,09:35,10:05,10:35,11:05,11:35,12:05
  kr_afternoon:
    if: ${{ (github.event_name == 'schedule' && github.event.schedule == '21 11 * * 1-5') || (github.event_name == 'workflow_dispatch' && (inputs.watch == 'all' || inputs.watch == 'kr-afternoon')) }}
    uses: ./.github/workflows/run-market-session-watcher.yml
    with:
      market: kr
      session_name: afternoon
      slots: 12:35,13:05,13:35,14:05,14:35,15:05,15:30
  cn_morning:
    if: ${{ (github.event_name == 'schedule' && github.event.schedule == '50 8 * * 1-5') || (github.event_name == 'workflow_dispatch' && (inputs.watch == 'all' || inputs.watch == 'cn-morning')) }}
    uses: ./.github/workflows/run-market-session-watcher.yml
    with:
      market: cn
      session_name: morning
      slots: 09:35,10:05,10:35,11:05,11:30
  cn_afternoon:
    if: ${{ (github.event_name == 'schedule' && github.event.schedule == '20 12 * * 1-5') || (github.event_name == 'workflow_dispatch' && (inputs.watch == 'all' || inputs.watch == 'cn-afternoon')) }}
    uses: ./.github/workflows/run-market-session-watcher.yml
    with:
      market: cn
      session_name: afternoon
      slots: 13:05,13:35,14:05,14:35,15:00
  sg_morning:
    if: ${{ (github.event_name == 'schedule' && github.event.schedule == '22 8 * * 1-5') || (github.event_name == 'workflow_dispatch' && (inputs.watch == 'all' || inputs.watch == 'sg-morning')) }}
    uses: ./.github/workflows/run-market-session-watcher.yml
    with:
      market: sg
      session_name: morning
      slots: 09:05,09:35,10:05,10:35,11:05,11:35,12:00
  sg_afternoon:
    if: ${{ (github.event_name == 'schedule' && github.event.schedule == '21 12 * * 1-5') || (github.event_name == 'workflow_dispatch' && (inputs.watch == 'all' || inputs.watch == 'sg-afternoon')) }}
    uses: ./.github/workflows/run-market-session-watcher.yml
    with:
      market: sg
      session_name: afternoon
      slots: 13:05,13:35,14:05,14:35,15:05,15:35,16:05,16:35,17:00
  uk_morning:
    if: ${{ (github.event_name == 'schedule' && github.event.schedule == '20 7 * * 1-5') || (github.event_name == 'workflow_dispatch' && (inputs.watch == 'all' || inputs.watch == 'uk-morning')) }}
    uses: ./.github/workflows/run-market-session-watcher.yml
    with:
      market: uk
      session_name: morning
      slots: 08:05,08:35,09:05,09:35,10:05,10:35,11:05,11:35,12:05
  uk_afternoon:
    if: ${{ (github.event_name == 'schedule' && github.event.schedule == '22 11 * * 1-5') || (github.event_name == 'workflow_dispatch' && (inputs.watch == 'all' || inputs.watch == 'uk-afternoon')) }}
    uses: ./.github/workflows/run-market-session-watcher.yml
    with:
      market: uk
      session_name: afternoon
      slots: 12:35,13:05,13:35,14:05,14:35,15:05,15:35,16:05,16:30
  eu_morning:
    if: ${{ (github.event_name == 'schedule' && github.event.schedule == '23 8 * * 1-5') || (github.event_name == 'workflow_dispatch' && (inputs.watch == 'all' || inputs.watch == 'eu-morning')) }}
    uses: ./.github/workflows/run-market-session-watcher.yml
    with:
      market: eu
      session_name: morning
      slots: 09:05,09:35,10:05,10:35,11:05,11:35,12:05,12:35,13:05
  eu_afternoon:
    if: ${{ (github.event_name == 'schedule' && github.event.schedule == '50 12 * * 1-5') || (github.event_name == 'workflow_dispatch' && (inputs.watch == 'all' || inputs.watch == 'eu-afternoon')) }}
    uses: ./.github/workflows/run-market-session-watcher.yml
    with:
      market: eu
      session_name: afternoon
      slots: 13:35,14:05,14:35,15:05,15:35,16:05,16:35,17:05,17:30
  us_morning:
    if: ${{ (github.event_name == 'schedule' && github.event.schedule == '51 8 * * 1-5') || (github.event_name == 'workflow_dispatch' && (inputs.watch == 'all' || inputs.watch == 'us-morning')) }}
    uses: ./.github/workflows/run-market-session-watcher.yml
    with:
      market: us
      session_name: morning
      slots: 09:35,10:05,10:35,11:05,11:35,12:05,12:35,13:05
  us_afternoon:
    if: ${{ (github.event_name == 'schedule' && github.event.schedule == '51 12 * * 1-5') || (github.event_name == 'workflow_dispatch' && (inputs.watch == 'all' || inputs.watch == 'us-afternoon')) }}
    uses: ./.github/workflows/run-market-session-watcher.yml
    with:
      market: us
      session_name: afternoon
      slots: 13:35,14:05,14:35,15:05,15:35,16:00
